@model IEnumerable<Odonit.Models.ObraSocial>

@{
    Layout = null;
    ViewBag.Title = "Index";
}
 
<h2>Index</h2>
    @*<a href="" ng-click="getData()">GET</a>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Denominacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Abreviatura)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Denominacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Abreviatura)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdObraSocial }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdObraSocial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdObraSocial })
        </td>
    </tr>
}

</table>